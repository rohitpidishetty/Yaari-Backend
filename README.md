Yaari's Backend - Completely re-designed using Spring Boot & MySQL, end points are secured by JTW.
Token Bucket Rate Limiter is added.
For AI/ML tasks Django microservices will be used.
Using JDBC instead of JPA for 100% controll over MySQL, queries are written in folder sql/*.sql
